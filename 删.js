/*
  db.collection.remove()
    -删除一个或者多个,可以第二个参数传递true，则只会删除一个
    -如果传递一个空对象作为参数，则删除全部
  db.collection.deleteOne()  
  db.collection.deleteMany() 
  db.collection.drop()删除几何
  db.dropDatabase()删除数据库 
*/
//具体不演示了，不然写的东西全没了。

//删除过的东西都在数据库可以找到。
